.env*
__pycache__/
/bkp/
/rust/target/
/harness
/harness.exe
# Estado vivo por-instalacion: nunca se versiona (cada proyecto tiene el suyo;
# el instalador lo siembra desde templates/ si falta)
/feature_list.json
/progress/
/graphify-out*
# Marker de layout: estado LOCAL de instalacion, no codigo (decision usuario
# 2026-07-28, feature #7). Versionado hacia que todo clon naciera declarando
# "mi raiz es mi padre" y el checkout fuente resolviera a $HOME. El instalador
# lo escribe en cada instalacion.
/.harness_layout
.harness.env

# Basura de macOS: nunca entra al repo (ni al commit del cierre).
.DS_Store
